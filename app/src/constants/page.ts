export const DEFAULT_PAGE = 1;
export const DEFAULT_PAGE_LENGTH = 1;
export const DEFAULT_PAGE_SIZE = 10;
export const MAX_PAGE_SIZE = 1000;
export const MIN_PAGE_SIZE = 20;
export const MINIMUM_PAGE_COUNT = 1;
export const PAGE_SIZE_OPTIONS = [10, 20, 30, 40];
export const MAX_PAGINATION_VALUE = PAGE_SIZE_OPTIONS[PAGE_SIZE_OPTIONS.length - 1];
export const MAX_PAGE_SIZE_OPTION = Math.max(...PAGE_SIZE_OPTIONS);

export const MAX_DEFAULT_PAGE_SIZE = 40;

export const PAGE_SIZE = {
  10: 10,
};
